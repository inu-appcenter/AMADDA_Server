// const myRes = require("../util/myResponse");
// const { onSendingMsgError } = require("../controller/logController");

// module.exports = {
//   isConnected: function(req, res, next) {
//     next();
//     // if (global.DBconnected) next();
//     // else {
//     //   res.status(500).json(myRes(false, "db 연결 실패"));
//     //   onSendingMsgError(new Error(), `${req.url}-> DB is not connected`);
//     // }
//   }
// };
